<!---This code isn't in use but thought it would be interesting to experiment (no link to access this page)--->

<h1><%= @post.title %></h1>

<p><%= @post.posted_on %></p>

<p>
<%= @post.description %>
</p>


<p><a href="/posts/<%= @post.place.id %>">Back to <%= @post.place.name %></a></p>